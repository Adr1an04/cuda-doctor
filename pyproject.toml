[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cuda-doctor"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cuda-doctor = "cuda_doctor.cli:main"

[tool.setuptools.packages.find]
include = ["cuda_doctor*"]
